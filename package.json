{
  "name": "@platforma-open/milaboratories.repertoire-distance-2",
  "version": "1.0.0",
  "scripts": {
    "fmt": "turbo run fmt",
    "check": "turbo run check",
    "build": "turbo run build",
    "build:dev": "env PL_PKG_DEV=local turbo run build",
    "test": "env PL_PKG_DEV=local turbo run test --concurrency 1 --env-mode=loose",
    "mark-stable": "turbo run mark-stable",
    "do-pack": "turbo run do-pack",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "watch": "turbo watch build",
    "update-sdk": "pl-blocks-deps-updater"
  },
  "files": [
    "index.d.ts",
    "index.js"
  ],
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@platforma-sdk/blocks-deps-updater": "catalog:",
    "turbo": "catalog:"
  },
  "packageManager": "pnpm@9.14.4"
}